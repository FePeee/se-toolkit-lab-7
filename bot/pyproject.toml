[project]
name = "se-toolkit-bot"
version = "0.1.0"
description = "Telegram bot scaffold with offline test mode"
requires-python = ">=3.12"
dependencies = ["aiogram>=3.20", "httpx>=0.28.1", "python-dotenv>=1.1.1"]
